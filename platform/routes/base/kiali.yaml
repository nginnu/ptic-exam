apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: kiali
  namespace: routes
spec:
  hostnames:
    - kiali-ptic.nginnu.com
  parentRefs:
    - name: external
      namespace: istio-system
  rules:
    - backendRefs:
        - name: kiali
          namespace: istio-system
          port: 20001
