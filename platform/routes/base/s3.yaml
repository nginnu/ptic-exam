apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: s3
  namespace: routes
spec:
  hostnames:
    - s3-ptic.nginnu.com
  parentRefs:
    - name: external
      namespace: istio-system
  rules:
    - backendRefs:
        - name: rustfs
          namespace: rustfs
          port: 9000
